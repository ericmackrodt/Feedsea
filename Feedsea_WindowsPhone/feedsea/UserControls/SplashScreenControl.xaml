<UserControl x:Class="feedsea.UserControls.SplashScreenControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}">
    
    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}">
        <Image Source="{Binding SplashScreenImage, Source={StaticResource MultiResImages}}" />
        <ProgressBar IsIndeterminate="True" VerticalAlignment="Bottom" Foreground="{StaticResource DefaultTextColor}"
                     Margin="50"/>
    </Grid>
</UserControl>